[project]
name = "church_sms"
authors = [
    { name = "KODA Systems", email = "justinemsengi@gmail.com" }
]
description = "Church SMS Management System for Frappe/ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    "requests",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
